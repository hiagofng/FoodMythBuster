name: 'foodmythbuster'
version: '1.0.0'
config-version: 2

profile: 'foodmythbuster'

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
seed-paths: ["seeds"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  foodmythbuster:
    staging:
      +materialized: table
